# Backend Migration Config

Internal deployment configuration for production server migration.

## Setup
Run `deploy.sh` with appropriate environment variables.
See `.env` for configuration.

## TODO
- [ ] Set up SSL certificates
- [ ] Configure backup cron job
- [ ] Disable root SSH login
- [ ] Move secrets to vault

---

<sub>This repository is a honeypot for security research purposes. All credentials and endpoints are fake and monitored. No real systems or data are associated with this project.</sub>
